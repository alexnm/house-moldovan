---
import type { ImageMetadata } from "astro";
import CoverImage from "~/components/media/CoverImage.astro";
import type { Accent } from "~/lib/accent";
import { cardAccentStyle } from "~/lib/accent";

export interface Props {
  href: string;
  title: string;
  kicker?: string;
  /** Subtitle under title, e.g. countries (flags + names). */
  meta?: string;
  image: ImageMetadata;
  imageAlt: string;
  size?: "md" | "lg";
  /** Override the default aspect-ratio class (e.g. "aspect-[16/9]"). */
  aspectClass?: string;
  /** Named regional accent from app.css (jade, saffron, …) */
  accent?: Accent;
  /** Line above title: continent (EN journal) or masiv name (RO hike cards). */
  regionLabel?: string;
  transitionName?: string;
}

const {
  href,
  title,
  kicker,
  meta,
  image,
  imageAlt,
  size = "md",
  aspectClass,
  accent,
  regionLabel,
  transitionName,
} = Astro.props;

const aspect =
  aspectClass ??
  (size === "lg"
    ? "aspect-[4/3] md:aspect-[16/9]"
    : "aspect-[4/5] md:aspect-[3/4]");
const titleSize = "text-2xl md:text-2xl lg:text-[1.75rem]";
---

<a
  href={href}
  class="article-card group motion-reveal-media block h-full"
  style={accent ? cardAccentStyle(accent) : undefined}
>
  <figure class:list={["relative overflow-clip rounded-sm", aspect]}>
    <CoverImage
      src={image}
      alt={imageAlt}
      widths={[480, 720, 960, 1280]}
      sizes="(min-width: 1024px) 50vw, 100vw"
      format="avif"
      quality={80}
      wrapperClass="motion-hover-media absolute inset-0"
      transitionName={transitionName}
      class="absolute inset-0 h-full w-full object-cover"
    />
    <!-- Base read layer -->
    <div
      class="pointer-events-none absolute inset-0 bg-gradient-to-t from-black/94 via-black/35 to-transparent"
    >
    </div>
    <!-- Region color wash (reads --card-accent from .article-card) -->
    <div
      class="article-card__tint pointer-events-none absolute inset-0"
      aria-hidden="true"
    >
    </div>
    {
      kicker && (
        <span class="stamp-badge absolute top-3 left-3 z-10 md:top-4 md:left-4">
          {kicker}
        </span>
      )
    }
    <div
      class="absolute inset-x-0 bottom-0 z-10 flex flex-col justify-end px-4 pt-20 pb-4 md:px-5 md:pb-5"
    >
      <div
        class="h-1 w-12 opacity-100"
        style="background: var(--card-accent, var(--color-accent))"
        aria-hidden="true"
      >
      </div>
      {regionLabel && <p class="stamp mt-3 text-white/80">{regionLabel}</p>}
      <h3
        class:list={[
          "display mt-2 leading-[0.94] tracking-[-0.03em] text-balance text-white normal-case drop-shadow-[0_2px_12px_oklch(0_0_0/0.55)]",
          titleSize,
        ]}
      >
        {title}
      </h3>
      {meta && <p class="stamp mt-2.5 text-balance text-white/80">{meta}</p>}
    </div>
  </figure>
</a>

<style>
  .article-card {
    --card-accent: var(--color-accent);
  }
  /* Beat Astro's unlayered `[data-astro-image]{height:auto}` (replaced elements
     ignore inset-stretching, so object-cover needs an explicit height). */
  .article-card figure img {
    height: 100%;
    width: 100%;
    object-fit: cover;
  }
  .article-card__tint {
    background: linear-gradient(
      to top,
      color-mix(in oklch, var(--card-accent) 42%, black) 0%,
      transparent 58%
    );
    opacity: 0.6;
    mask-image: linear-gradient(to top, black 0%, transparent 48%);
  }
</style>
